# V02 灯光控制验证

日期：2026-09-07。

- `npm run typecheck`：通过。
- `npm test`：20 / 20 通过，含新增灯光回归 6 项。
- 旧项目迁移：从 v1 还原原来三盏灯的所有默认参数；相机、对象、名称、时间戳及原输入不变。
- 镜头独立性：复制镜头后修改灯光，A/B 切换 20 次均精确恢复；项目文件编码解码后完全一致。
- 编辑历史：灯光更新与复位均可撤销重做，不影响对象或摄影机；相同灯光更新不污染保存标记。
- 数据校验：拒绝缺失的 v2 灯光、非法颜色、非数值强度、超出范围和主光方向退化；关灯合法。
- 本机恢复：保留 v1 原始字符串，写入独立 v2 key；损坏的 v2 不回退、不覆盖。
- 渲染结构：编辑与拍摄共用 LightingRig；灯光 target 是各自 scene 的独立子对象，同步参与矩阵更新；PNG 使用拍摄 scene；改变灯距时更新阴影投影矩阵。

本轮没有重新运行交互式浏览器 PNG 检查；V01 的像素验证记录见 VALIDATION.md。可在 `/verify` 手动运行真实 PNG 验证。上述渲染一致性本轮基于共享代码路径与编译检查，不冒充本轮像素实测。
