# V01 验证记录

日期：2026-09-07。环境：Windows、本机 Node 24.17.0、127.0.0.1:8766 正式静态构建、支持 WebGL 的浏览器。

## 自动化验证

- `npm run typecheck`：通过。
- `npm test`：14 / 14 通过。包括镜头 A/B 往返 20 次及项目编码/解码恢复、一次拖拽的撤销事务、取消后的迟到事件、撤销重做、导航保留重做、摄影机锁定、画幅计算及固定导出尺寸。
- `npm run build`：成功退出，生成 `/` 与 `/verify`。存在 Three.js 客户端较大分包提示，不影响运行。
- 正式版 `/verify`：10 / 10 通过。实际 WebGL 渲染、PNG 解码验证两个画幅，非空画面、重复 PNG 哈希一致、投影矩阵一致、无辅助对象，且导出不改动摄影机。

## 浏览器操作回归

- 创建第二镜头，将人物 A 的 X 改为 2.25、摄影机改为 50 mm / 9:16；切回第一镜头仍为 X=-1.15、35 mm / 16:9。再次切回第二镜头恢复准确，正式页面刷新后两镜头数据均保留。
- 两次点击真实 PNG 导出，读取生成图片尺寸分别为 1920×1080、1080×1920。
- 实际拖动人物 A 的 X 手柄：X 从 -1.15 到 0.04150332933990408；拍摄摄影机数据未变化，编辑视窗的墙体/地面保持原来的视角。一次撤销使整个项目数据与拖动前完全一致，一次重做与拖动后完全一致。最后撤销恢复示例。
- 修复松开三轴手柄时误选底下地面的问题；回归确认松手后仍选中人物 A。
- WebMCP 只读项目查询与有效镜头切换通过；无效镜头 ID 被拒绝。
- 启动脚本 PowerShell 语法解析 0 错误；主页 HTTP 200；静态服务拒绝不可信 Host 与目录越界路径。

## 边界

没有调用付费生成接口，没有测试时间线或 AI。验证基于当前机器与浏览器，不代表对所有 GPU / 浏览器的兼容性保证。项目文件备份不包含撤销栈。
